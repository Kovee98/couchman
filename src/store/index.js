import Vue from 'vue';
import Vuex from 'vuex';
import connections from './connections';

Vue.use(Vuex);

export default new Vuex.Store({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        connections
    }
});
